const defaultValidations = {
  // eslint-disable-next-line no-unused-vars
  headers: (_headers) => {},
  // eslint-disable-next-line no-unused-vars
  query: (_query) => {},
  // eslint-disable-next-line no-unused-vars
  body: (_body) => {},
};

module.exports = defaultValidations;
